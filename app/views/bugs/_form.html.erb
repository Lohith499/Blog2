<%= form_with(model: bug, local: true) do |form| %>
  <% if bug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bug.errors.count, "error") %> prohibited this bug from being saved:</h2>

      <ul>
      <% bug.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :bug_title %>
  </div>

  <div class="field">
    <%= form.label :articles %>
    <%= form.text_area :articles, id: :bug_articles %>
  </div>

  <div class="field">
    <%= form.label :issue_type %>
    <%= form.text_area :issue_type, id: :bug_issue_type %>
  </div>

  <div class="field">
    <%= form.label :priority %>
    <%= form.text_area :priority, id: :bug_priority %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_area :status, id: :bug_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
